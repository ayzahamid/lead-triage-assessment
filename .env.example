# Postgres connection string. Optional: without it the app uses an in-memory store.
# DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# Model API keys are never set here. Each person supplies their own in the app.
